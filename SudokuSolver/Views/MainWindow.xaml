<local:SubClassWindow
    x:Class="Sudoku.Views.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Sudoku.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <local:CustomTitleBar x:Name="customTitleBar" TitleBarTheme="{x:Bind puzzleView.ViewModel.Theme, Mode=OneWay}"/>

        <Grid Grid.Row="1" x:Name="clientArea" RequestedTheme ="{x:Bind puzzleView.ViewModel.Theme, Mode=OneWay}" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <MenuBar>
                <MenuBarItem Title="File" AccessKey="F">
                    <MenuFlyoutItem>
                        <MenuFlyoutItem.Command>
                            <StandardUICommand Kind="Open" ExecuteRequested="OpenCommand_ExecuteRequested" AccessKey="N"/>
                        </MenuFlyoutItem.Command>
                    </MenuFlyoutItem>

                    <MenuFlyoutItem Text="Clear" Command="{x:Bind puzzleView.ViewModel.ClearCommand}" AccessKey="C" KeyboardAcceleratorTextOverride ="Ctrl+C">
                        <MenuFlyoutItem.Icon>
                            <SymbolIcon Symbol="Clear"/>
                        </MenuFlyoutItem.Icon>
                    </MenuFlyoutItem>

                    <MenuFlyoutSeparator />
                    <MenuFlyoutItem>
                        <MenuFlyoutItem.Command>
                            <StandardUICommand Kind="Save" ExecuteRequested="SaveCommand_ExecuteRequested" AccessKey="S"/>
                        </MenuFlyoutItem.Command>
                    </MenuFlyoutItem>

                    <MenuFlyoutItem Text="Save As..." Click="SaveAsClickHandler" AccessKey="A" KeyboardAcceleratorTextOverride ="Ctrl+Shift+S"/>

                    <MenuFlyoutSeparator />
                    <MenuFlyoutItem Text="Print..." Click="PrintClickHandler" IsEnabled="{x:Bind IsPrintingAvailable}" AccessKey="P" KeyboardAcceleratorTextOverride ="Ctrl+P">
                        <MenuFlyoutItem.Icon>
                            <SymbolIcon Symbol="Print"/>
                        </MenuFlyoutItem.Icon>
                    </MenuFlyoutItem>

                    <MenuFlyoutSeparator />
                    <MenuFlyoutItem Text="Exit" Click="ExitClickHandler" AccessKey="E" KeyboardAcceleratorTextOverride ="Alt+F4">
                        <MenuFlyoutItem.Icon>
                            <SymbolIcon Symbol="ClosePane"/>
                        </MenuFlyoutItem.Icon>
                    </MenuFlyoutItem>
                </MenuBarItem>

                <MenuBarItem Title="Options" AccessKey="O">

                    <ToggleMenuFlyoutItem Text="Show possibles" IsChecked="{x:Bind puzzleView.ViewModel.ShowPossibles, Mode=TwoWay}" AccessKey="P"/>
                    <ToggleMenuFlyoutItem Text="Show solution" IsChecked="{x:Bind puzzleView.ViewModel.ShowSolution, Mode=TwoWay}" AccessKey="S"/>

                    <MenuFlyoutSeparator />
                    <ToggleMenuFlyoutItem Text="Dark theme" IsChecked="{x:Bind puzzleView.ViewModel.IsDarkThemed, Mode=TwoWay}" AccessKey="D"/>

                    <MenuFlyoutSeparator />
                    <MenuFlyoutItem Text="About..." Click="AboutClickHandler" AccessKey="A">
                    </MenuFlyoutItem>
                </MenuBarItem>
            </MenuBar>

            <local:PuzzleView x:Name="puzzleView" Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="8,0,8,8"/>
        </Grid>
    </Grid>
</local:SubClassWindow>
