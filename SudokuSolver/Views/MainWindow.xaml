<local:SubClassWindow
    x:Class="Sudoku.Views.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Sudoku.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid x:Name="LayoutRoot" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid x:Name="CustomTitleBar" Height="32">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <ImageIcon Source="{x:Bind local:MainWindow.LoadWindowIconImage(), Mode=OneTime}" Width="16" Height="16" Margin="12,0,0,0"/>
            <TextBlock Grid.Column="1" x:Name="WindowTitle" Margin="8,0,0,0" VerticalAlignment="Center"/>
        </Grid>

        <MenuBar Grid.Row="1">
            <MenuBarItem Title="File" AccessKey="F">
                <MenuFlyoutItem>
                    <MenuFlyoutItem.Command>
                        <StandardUICommand Kind="Open" ExecuteRequested="OpenCommand_ExecuteRequested" AccessKey="N"/>
                    </MenuFlyoutItem.Command>
                </MenuFlyoutItem>

                <MenuFlyoutItem Text="Clear" Command="{x:Bind PuzzleView.ViewModel.ClearCommand}" AccessKey="C" KeyboardAcceleratorTextOverride ="Ctrl+C">
                    <MenuFlyoutItem.Icon>
                        <SymbolIcon Symbol="Clear"/>
                    </MenuFlyoutItem.Icon>
                </MenuFlyoutItem>

                <MenuFlyoutSeparator />
                <MenuFlyoutItem>
                    <MenuFlyoutItem.Command>
                        <StandardUICommand Kind="Save" ExecuteRequested="SaveCommand_ExecuteRequested" CanExecuteRequested="SaveCommand_CanExecuteRequested" AccessKey="S"/>
                    </MenuFlyoutItem.Command>
                </MenuFlyoutItem>

                <MenuFlyoutItem Text="Save As..." Click="SaveAsClickHandler" AccessKey="A" KeyboardAcceleratorTextOverride ="Ctrl+Shift+S"/>

                <MenuFlyoutSeparator />
                <MenuFlyoutItem Text="Print..." Click="PrintClickHandler" IsEnabled="{x:Bind local:MainWindow.IsPrintingSupported}" AccessKey="P" KeyboardAcceleratorTextOverride ="Ctrl+P">
                    <MenuFlyoutItem.Icon>
                        <SymbolIcon Symbol="Print"/>
                    </MenuFlyoutItem.Icon>
                </MenuFlyoutItem>

                <MenuFlyoutSeparator />
                <MenuFlyoutItem Text="Exit" Click="ExitClickHandler" AccessKey="E" KeyboardAcceleratorTextOverride ="Alt+F4">
                    <MenuFlyoutItem.Icon>
                        <SymbolIcon Symbol="ClosePane"/>
                    </MenuFlyoutItem.Icon>
                </MenuFlyoutItem>
            </MenuBarItem>

            <MenuBarItem Title="Options" AccessKey="O">

                <ToggleMenuFlyoutItem Text="Show possibles" IsChecked="{x:Bind PuzzleView.ViewModel.ShowPossibles, Mode=TwoWay}" AccessKey="P"/>
                <ToggleMenuFlyoutItem Text="Show solution" IsChecked="{x:Bind PuzzleView.ViewModel.ShowSolution, Mode=TwoWay}" AccessKey="S"/>

                <MenuFlyoutSeparator />
                <ToggleMenuFlyoutItem Text="Dark theme" IsChecked="{x:Bind PuzzleView.ViewModel.IsDarkThemed, Mode=TwoWay}" AccessKey="D"/>

                <MenuFlyoutSeparator />
                <MenuFlyoutItem Text="About" Click="AboutClickHandler" AccessKey="A" KeyboardAcceleratorTextOverride ="Ctrl+A">
                    <MenuFlyoutItem.Icon>
                        <SymbolIcon Symbol="ContactInfo"/>
                    </MenuFlyoutItem.Icon>
                </MenuFlyoutItem>
            </MenuBarItem>
        </MenuBar>

        <local:PuzzleView x:Name="PuzzleView" Grid.Row="2" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="8,0,8,8"/>
    </Grid>
</local:SubClassWindow>
