<UserControl x:Class="SudokuSolver.Views.Cell"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="using:SudokuSolver.Views"
             Height="{StaticResource cell_size}"
             Width="{StaticResource cell_size}">

    <Canvas x:Name="CellCanvas" Height="{StaticResource cell_size}" Width="{StaticResource cell_size}">

        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="CellValueStates">
                <VisualState x:Name="Calculated">
                    <VisualState.Setters>
                        <Setter Target="CellValue.Foreground" Value="{ThemeResource CalculatedCellBrush}"/>
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="Trial">
                    <VisualState.Setters>
                        <Setter Target="CellValue.Foreground" Value="{ThemeResource TrialCellBrush}"/>
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="Provided">
                    <VisualState.Setters>
                        <Setter Target="CellValue.Foreground" Value="{ThemeResource ProvidedCellBrush}"/>
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="User"/>
            </VisualStateGroup>

            <VisualStateGroup x:Name="CommonStates">
                <VisualState x:Name="SelectedFocused">
                    <VisualState.Setters>
                        <Setter Target="CellCanvas.Background" Value="{ThemeResource SystemControlHighlightAccentBrush}"/>
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="SelectedUnfocused">
                    <VisualState.Setters>
                        <Setter Target="CellCanvas.Background" Value="{ThemeResource SystemControlBackgroundListLowBrush}"/>
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="Normal"/>
            </VisualStateGroup>

            <VisualStateGroup x:Name="PossibleStates1">
                <VisualState x:Name="Horizontal1">
                    <VisualState.Setters>
                        <Setter Target="PossibleValue1.Foreground" Value="{ThemeResource PossiblesHorizontalBrush}"/>
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="Vertical1">
                    <VisualState.Setters>
                        <Setter Target="PossibleValue1.Foreground" Value="{ThemeResource PossiblesVerticalBrush}"/>
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="Normal1"/>
            </VisualStateGroup>

            <VisualStateGroup x:Name="PossibleStates2">
                <VisualState x:Name="Horizontal2">
                    <VisualState.Setters>
                        <Setter Target="PossibleValue2.Foreground" Value="{ThemeResource PossiblesHorizontalBrush}"/>
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="Vertical2">
                    <VisualState.Setters>
                        <Setter Target="PossibleValue2.Foreground" Value="{ThemeResource PossiblesVerticalBrush}"/>
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="Normal2"/>
            </VisualStateGroup>

            <VisualStateGroup x:Name="PossibleStates3">
                <VisualState x:Name="Horizontal3">
                    <VisualState.Setters>
                        <Setter Target="PossibleValue3.Foreground" Value="{ThemeResource PossiblesHorizontalBrush}"/>
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="Vertical3">
                    <VisualState.Setters>
                        <Setter Target="PossibleValue3.Foreground" Value="{ThemeResource PossiblesVerticalBrush}"/>
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="Normal3"/>
            </VisualStateGroup>

            <VisualStateGroup x:Name="PossibleStates4">
                <VisualState x:Name="Horizontal4">
                    <VisualState.Setters>
                        <Setter Target="PossibleValue4.Foreground" Value="{ThemeResource PossiblesHorizontalBrush}"/>
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="Vertical4">
                    <VisualState.Setters>
                        <Setter Target="PossibleValue4.Foreground" Value="{ThemeResource PossiblesVerticalBrush}"/>
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="Normal4"/>
            </VisualStateGroup>
            
            <VisualStateGroup x:Name="PossibleStates5">
                <VisualState x:Name="Horizontal5">
                    <VisualState.Setters>
                        <Setter Target="PossibleValue5.Foreground" Value="{ThemeResource PossiblesHorizontalBrush}"/>
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="Vertical5">
                    <VisualState.Setters>
                        <Setter Target="PossibleValue5.Foreground" Value="{ThemeResource PossiblesVerticalBrush}"/>
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="Normal5"/>
            </VisualStateGroup>

            <VisualStateGroup x:Name="PossibleStates6">
                <VisualState x:Name="Horizontal6">
                    <VisualState.Setters>
                        <Setter Target="PossibleValue6.Foreground" Value="{ThemeResource PossiblesHorizontalBrush}"/>
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="Vertical6">
                    <VisualState.Setters>
                        <Setter Target="PossibleValue6.Foreground" Value="{ThemeResource PossiblesVerticalBrush}"/>
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="Normal6"/>
            </VisualStateGroup>
            
            <VisualStateGroup x:Name="PossibleStates7">
                <VisualState x:Name="Horizontal7">
                    <VisualState.Setters>
                        <Setter Target="PossibleValue7.Foreground" Value="{ThemeResource PossiblesHorizontalBrush}"/>
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="Vertical7">
                    <VisualState.Setters>
                        <Setter Target="PossibleValue7.Foreground" Value="{ThemeResource PossiblesVerticalBrush}"/>
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="Normal7"/>
            </VisualStateGroup>

            <VisualStateGroup x:Name="PossibleStates8">
                <VisualState x:Name="Horizontal8">
                    <VisualState.Setters>
                        <Setter Target="PossibleValue8.Foreground" Value="{ThemeResource PossiblesHorizontalBrush}"/>
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="Vertical8">
                    <VisualState.Setters>
                        <Setter Target="PossibleValue8.Foreground" Value="{ThemeResource PossiblesVerticalBrush}"/>
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="Normal8"/>
            </VisualStateGroup>
            
            <VisualStateGroup x:Name="PossibleStates9">
                <VisualState x:Name="Horizontal9">
                    <VisualState.Setters>
                        <Setter Target="PossibleValue9.Foreground" Value="{ThemeResource PossiblesHorizontalBrush}"/>
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="Vertical9">
                    <VisualState.Setters>
                        <Setter Target="PossibleValue9.Foreground" Value="{ThemeResource PossiblesVerticalBrush}"/>
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="Normal9"/>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>

        <TextBlock x:Name="CellValue" FontSize="34" Foreground="{ThemeResource UserCellBrush}" TextAlignment="Center" Width="{StaticResource cell_size}" Height="{StaticResource cell_size}"/>

        <TextBlock x:Name="PossibleValue1" Text="1" Foreground="{ThemeResource CellPossiblesBrush}" Canvas.Top="-2" Canvas.Left="4"/>
        <TextBlock x:Name="PossibleValue2" Text="2" Foreground="{ThemeResource CellPossiblesBrush}" Canvas.Top="-2" Canvas.Left="22"/>
        <TextBlock x:Name="PossibleValue3" Text="3" Foreground="{ThemeResource CellPossiblesBrush}" Canvas.Top="-2" Canvas.Left="39"/>
        <TextBlock x:Name="PossibleValue4" Text="4" Foreground="{ThemeResource CellPossiblesBrush}" Canvas.Top="15" Canvas.Left="4"/>
        <TextBlock x:Name="PossibleValue5" Text="5" Foreground="{ThemeResource CellPossiblesBrush}" Canvas.Top="15" Canvas.Left="22"/>
        <TextBlock x:Name="PossibleValue6" Text="6" Foreground="{ThemeResource CellPossiblesBrush}" Canvas.Top="15" Canvas.Left="39"/>
        <TextBlock x:Name="PossibleValue7" Text="7" Foreground="{ThemeResource CellPossiblesBrush}" Canvas.Top="31" Canvas.Left="4"/>
        <TextBlock x:Name="PossibleValue8" Text="8" Foreground="{ThemeResource CellPossiblesBrush}" Canvas.Top="31" Canvas.Left="22"/>
        <TextBlock x:Name="PossibleValue9" Text="9" Foreground="{ThemeResource CellPossiblesBrush}" Canvas.Top="31" Canvas.Left="39"/>
    </Canvas>
</UserControl>