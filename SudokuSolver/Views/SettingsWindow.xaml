<?xml version="1.0" encoding="utf-8"?>
<local:WindowBase
    x:Class="SudokuSolver.Views.SettingsWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SudokuSolver.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"    
    MinWidth="400"
    MinHeight="300"
    InitialWidth="400"
    InitialHeight="300">

    <Grid x:Name="layoutRoot"> <!--RequestedTheme="{x:Bind ViewModel.Theme, Mode=OneWay}">-->

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <local:CustomTitleBar x:Name="CustomTitleBar" IconSourceUri="ms-appx:///Resources/settings.png"/>

        <local:SimpleColorPicker Grid.Row="1" MiniPalette="False" Color="{x:Bind UserColor, Mode=TwoWay}"/>
        <local:SimpleColorPicker Grid.Row="2" MiniPalette="True" Color="{x:Bind CalculatedColor}" ColorChanged="{x:Bind CalculatedColorChangedEventHndler}"/>

        <Button Grid.Row="5" Click="ButtonClick" Content="Dark mode"/>

    </Grid>
</local:WindowBase>
