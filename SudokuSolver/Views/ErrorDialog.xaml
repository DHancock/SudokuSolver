<ContentDialog
    x:Class="Sudoku.Views.ErrorDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Sudoku.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Style="{StaticResource DefaultContentDialogStyle}"
    Title="{x:Null}"
    PrimaryButtonText = "OK">
    
    <ContentDialog.Content>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition MaxWidth="325"/>
            </Grid.ColumnDefinitions>

            <Image x:Name="errorImage" Width="62" Height="62" Margin="0,7,20,0" VerticalAlignment="Top"/>

            <StackPanel Grid.Column="1">
                <TextBlock x:Name="title" FontSize="24" TextWrapping="Wrap"/>
                <TextBlock x:Name="message" TextWrapping="Wrap"/>
            </StackPanel>
        </Grid>
    </ContentDialog.Content>
</ContentDialog>
